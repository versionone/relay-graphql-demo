#!/bin/sh -e

if ! [ -z "$1" ]; then
	NODE_ENV=test TEST_GREP="$1" `npm bin`/karma start ./build/config/karma/karma.config.js && \
	`npm bin`/karma run
else
	NODE_ENV=test `npm bin`/karma start ./build/config/karma/karma.config.js && \
	`npm bin`/karma run
fi