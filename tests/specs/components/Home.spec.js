import sinon from 'sinon';
import Sut from './../../../src/app/components/root/Html';

describe('src/app/components/root/Html', function() {
	beforeEach(() => {
		this.sut = null;
	});
	describe('when loading this test', () => {
		it('it should pass', () => {
			true.should.be.truthy;
		});
	});
});
